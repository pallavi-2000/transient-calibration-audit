# ====================================================================
# Calibration Audit — Pinned Dependencies
# ====================================================================
# Exact versions used to produce the results in:
#   Sati 2026 — "Calibration Audit of Production Astronomical
#   Transient Classifiers on ZTF Alert Streams"
#
# A complete environment freeze (all transitive dependencies) is in
# requirements_frozen_full.txt.
#
# Reproduction:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# Python version: 3.8.0 (CPython)
# Platform: macOS 24.5.0 (Darwin)
# ====================================================================

# Core scientific stack
numpy==1.24.4
scipy==1.10.1
pandas==2.0.3
matplotlib==3.7.5

# Statistical methods (scripts 11, 14, 15: Platt/isotonic/chi-sq)
scikit-learn==1.3.2

# API and HTTP (ALeRCE/Fink broker queries)
requests==2.32.4

# Astronomy / FITS handling
astropy==5.2.2

# Progress bars
tqdm==4.67.1

# Testing (tests/) -- not required to reproduce paper numbers,
# only to run the pytest suite
pytest==8.3.5
